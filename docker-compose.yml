services:
  giusecoder:
    build: .
    ports:
      - "4000:4000"
    environment:
      - HOST=0.0.0.0
      - PORT=4000
      - PROJECT_ROOT=/workspace
    volumes:
      - ./workspace:/workspace
    restart: unless-stopped
